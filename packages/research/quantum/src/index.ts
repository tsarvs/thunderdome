/**
 * @thunderdome/research-quantum — intentionally a skeleton, not a domain model (see README.md).
 * Proves research-core's schema is generic across a second, unrelated domain.
 */
export * from './fixture.js';
